# Server Configuration
ADMIN_KEY=your-secure-admin-key-here
PORT=8080
LOG_FILE=/app/logs/govee_server.log
STORAGE_DIR=/app/data
CLIENT_TIMEOUT=5m
HTTPS=true
CERT=/app/certs/cert.pem
KEY=/app/certs/key.pem
AUTH=true
ALLOW_DEFAULT=false

# InfluxDB Configuration (if using optional InfluxDB service)
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=change-me-to-secure-password
INFLUXDB_ADMIN_TOKEN=change-me-to-secure-token

# Grafana Configuration (if using optional Grafana service)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-to-secure-password

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with secure random strings
# 3. NEVER commit .env to version control
# 4. Ensure .env is listed in .gitignore
